import 'package:flutter/material.dart';

class Tablet_layout extends StatelessWidget {
  const Tablet_layout({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}