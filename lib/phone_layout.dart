import 'package:flutter/material.dart';

class Phone_layout extends StatelessWidget {
  const Phone_layout({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: <Widget>[
          Image.asset('images/light.png')
        ],
      ),
    );
  }
}